# Repository-wide rustfmt policy. Values intentionally match rustfmt defaults
# where possible so the policy is explicit without causing unrelated churn.
edition = "2021"
max_width = 100
newline_style = "Unix"
use_small_heuristics = "Default"
